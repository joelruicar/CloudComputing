apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: oauth2-proxy-claim0
  name: oauth2-proxy-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
